
# Astartes Forge v3.0.11 — Single Physical Renderer

- Space Wolves frame artwork is forced to the exact physical outer page edges.
- Frame is one single image behind all live content, so it can never cover text.
- Print content is mathematically centered.
- Theme Preview is a scale transform of the exact same print-card DOM.
- Theme controls use delegated change events and rebuild preview immediately.
- Theme tab print button removed.
- Normal Datasheets remain artwork-free.
